# Flask – SQLAlchemy (Szkolenie 5)

Kompletny projekt Flask z SQLAlchemy: model User, logowanie/rejestracja, dashboard z edycją emaila, baza SQLite.

## Uruchomienie

```bash
pip install -r requirements.txt
python run.py
```

Aplikacja: http://localhost:5000/login
