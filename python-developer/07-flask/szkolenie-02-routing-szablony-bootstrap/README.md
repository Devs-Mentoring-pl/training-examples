# Flask – Routing, szablony HTML, Bootstrap (Szkolenie 2)

Aplikacja Flask z routingiem, parametrami URL, szablonami Jinja2, dziedziczeniem szablonów i Bootstrapem.

## Uruchomienie

```bash
pip install flask
export FLASK_APP=app
flask --debug run
```

Aplikacja: http://localhost:5000
